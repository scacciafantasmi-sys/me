/* ==========================================================================
   VILLA FRANCA 1980 — bootstrap
   ========================================================================== */
document.addEventListener('DOMContentLoaded', () => {
  ENGINE.init();
  UI.init();
});
